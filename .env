# Enviguide Frontend — local environment
# API base URL the frontend calls. Points at the local backend (hardcoded to port 8000).
# Change to a deployed URL (e.g. https://enviguide.nextechltd.in) to target a remote backend.
VITE_API_BASE_URL=http://localhost:8000
